{
  "name": "@beresio/backend",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.3",
    "@beresio/db": "workspace:*",
    "better-auth": "^1.1.20",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "5.9.2",
    "wrangler": "^4.4.0"
  }
}
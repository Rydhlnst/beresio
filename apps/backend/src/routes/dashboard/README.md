# Dashboard Routes (Backend)

Catatan ringkas untuk route dashboard di backend.

- Prefix: `/api/dashboard/*`
- Semua route wajib auth.
- Laundry dashboard berada di `/api/dashboard/laundry/*`.
- Order intake (customer) diverifikasi di `/api/dashboard/laundry/order-intakes/*`.
